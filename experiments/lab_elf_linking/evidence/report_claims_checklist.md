# 报告结论证据检查表

| 报告结论 | 是否有证据 | 证据编号/文件 | 是否需要补证 | 备注 |
|---|---|---|---|---|
| 工作副本已按学号 `U202414558`、尾号 `8`、用户确认的 `zjl` 修改 | 是 | E004、E005 | 否 | 原始材料未修改 |
| `-D U8` 与学号末位匹配，程序可正常运行 | 是 | E010 | 否 | 三个可执行文件运行 exit_code=0 |
| 初始化全局变量进入 `.data`，未初始化全局变量进入 `.bss` | 是 | E008、E011、E012、E014 | 否 | 结合汇编、节头表、符号表和十六进制转储 |
| `.data.rel.local`、`.data.rel` 保存需要地址重定位的数据引用 | 是 | E013、E014 | 否 | `init_gp1`、`init_gp2`、`init_gfp1` |
| `main.o` 中外部符号在符号表中为 `UND` | 是 | E012 | 否 | 包括 `fadd`、`printf`、`extern_int` 等 |
| 链接前 call/全局地址引用保留重定位记录，链接后写入实际位移 | 是 | E013、E016 | 否 | 对比 `main.o` 和 `main_sub` 的 main 反汇编 |
| `main_sub` 与 `sub_main` 的函数/数据排列顺序受链接输入顺序影响 | 是 | E018 | 否 | `nm -n` 过滤输出 |
| 静态链接文件明显大于动态链接文件，且 `ldd` 不再显示动态依赖 | 是 | E018、E019、E021 | 否 | 动态 20312 字节，静态 789616 字节 |
| `printf` 通过动态符号、PLT/GOT、重定位项和 libc 运行时地址定位 | 是 | E019、E020 | 否 | GDB 记录为一次运行时地址证据 |
