# 调试与输出保存

请对当前实验进行调试或命令分析。要求：

1. GDB、readelf、objdump、hexdump 等输出必须保存到 `outputs/`。
2. 对关键地址、寄存器、节、符号、重定位项进行解释。
3. 把解释写入 `notes/debug_notes.md` 或 `notes/result_analysis.md`。
4. 报告中可使用的结论必须登记到 `evidence/report_claims_checklist.md`。
