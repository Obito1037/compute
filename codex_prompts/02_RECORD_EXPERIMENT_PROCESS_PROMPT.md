# 记录实验过程

请在执行任何命令前后完成记录：

1. 将命令、目的、当前目录、输出文件写入 `evidence/commands.md`。
2. 使用 `tee` 或重定向保存输出到 `outputs/`。
3. 如果命令失败，将错误写入 `evidence/errors_and_fixes.md`。
4. 如果需要截图，提醒我手动截图并保存到 `screenshots/`，然后登记到 `screenshots_index.md`。
5. 每完成一个关键步骤，更新 `evidence/evidence_manifest.md`。
