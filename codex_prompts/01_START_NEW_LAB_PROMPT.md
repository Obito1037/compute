# 开始一个实验

请开始处理指定实验目录：`experiments/<lab>/`。

步骤：
1. 阅读该实验的 `requirement_summary.md`。
2. 检查 `src/` 是否已有可操作源码；如没有，从 `original_materials/extracted/` 复制。
3. 填写 `evidence/environment.md`。
4. 生成本实验的执行计划，写入 `notes/principle_notes.md`。
5. 不要直接写报告正文。
